%YAML:1.0

# The directory containing all the meshes and .scene file
# Note: The path is relative to this YAML file
scene_dir: "ogre"

# The .scene file name
scene_file: "hand2j.scene"

# The name of the hand object entity in the scene
hand_object_name: hand

# The bone map:
# Each entry corresponds to the bone name in the armature of the hand mesh
bone_map:
   - "metacarpals"    # Will be joint(0) in the FullHandPose class
   - "finger1joint1"  # Will be joint(1) in the FullHandPose class
   - "finger1joint2"  # Will be joint(2) in the FullHandPose class
   - "finger1joint3"  #    .. and so on ..
   - "finger2joint1"
   - "finger2joint2"
   - "finger2joint3"
